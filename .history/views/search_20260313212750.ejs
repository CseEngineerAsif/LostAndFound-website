<div class="row mb-4 align-items-center">
  <div class="col-md-8">
    <h1>Search listings</h1>
    <p class="lead text-muted">Filter and browse reported lost and found items across campus.</p>
  </div>
  <div class="col-md-4 text-md-end mt-3 mt-md-0">
    <a href="/items/report" class="btn btn-outline-primary">
      <i class="bi bi-journal-plus me-2"></i>Report an item
    </a>
  </div>
</div>

<div class="card p-4 mb-4 shadow-sm">
  <form class="row g-3" method="get" action="/items/search">
    <div class="col-md-4">
      <div class="input-group">
        <span class="input-group-text"><i class="bi bi-search"></i></span>
        <input
          type="text"
          name="q"
          value="<%= filters.q || '' %>"
          class="form-control"
          placeholder="Search by item name or description"
        />
      </div>
    </div>
    <div class="col-md-2">
      <select name="category" class="form-select">
        <option value="">All categories</option>
        <option value="Phone" <%= filters.category === 'Phone' ? 'selected' : '' %>>Phone</option>
        <option value="Wallet" <%= filters.category === 'Wallet' ? 'selected' : '' %>>Wallet</option>
        <option value="Bag" <%= filters.category === 'Bag' ? 'selected' : '' %>>Bag</option>
        <option value="ID Card" <%= filters.category === 'ID Card' ? 'selected' : '' %>>ID Card</option>
        <option value="Books" <%= filters.category === 'Books' ? 'selected' : '' %>>Books</option>
        <option value="Electronics" <%= filters.category === 'Electronics' ? 'selected' : '' %>>Electronics</option>
        <option value="Others" <%= filters.category === 'Others' ? 'selected' : '' %>>Others</option>
      </select>
    </div>
    <div class="col-md-2">
      <div class="input-group">
        <span class="input-group-text"><i class="bi bi-geo-alt"></i></span>
        <input
          type="text"
          name="location"
          value="<%= filters.location || '' %>"
          class="form-control"
          placeholder="Location"
        />
      </div>
    </div>
    <div class="col-md-2">
      <select name="type" class="form-select">
        <option value="">Both</option>
        <option value="lost" <%= filters.type === 'lost' ? 'selected' : '' %>>Lost</option>
        <option value="found" <%= filters.type === 'found' ? 'selected' : '' %>>Found</option>
      </select>
    </div>
    <div class="col-md-2 d-grid">
      <button class="btn btn-primary" type="submit">
        <i class="bi bi-filter-circle me-1"></i>Filter
      </button>
    </div>
  </form>
</div>

<div class="row row-cols-1 row-cols-md-3 g-4 cards-grid">
  <% if (items && items.length) { %>
  <% items.forEach((item) => { %>
  <div class="col">
    <div class="card h-100">
      <% if (item.photoPath) { %>
      <img src="<%= item.photoPath %>" class="card-img-top" alt="Item photo" />
      <% } else { %>
      <div class="card-img-top bg-secondary text-white p-4 text-center">
        <i class="bi bi-camera" style="font-size: 2rem"></i>
        <div class="small mt-2">No photo provided</div>
      </div>
      <% } %>
      <div class="card-body d-flex flex-column">
        <div class="d-flex justify-content-between align-items-start mb-2">
          <h5 class="card-title mb-0"><%= item.name %></h5>
          <span class="badge bg-light text-dark text-uppercase small">
            <%= item.type %>
          </span>
        </div>
        <p class="card-text text-muted mb-3"><%= item.description ? item.description.slice(0, 90) : 'No description provided.' %></p>
        <div class="mt-auto">
          <p class="mb-2 small text-muted">
            <i class="bi bi-geo-alt me-1"></i><%= item.location %><br />
            <i class="bi bi-calendar-event me-1"></i><%= new Date(item.createdAt).toLocaleDateString() %>
          </p>
          <a href="/items/<%= item.id %>" class="btn btn-sm btn-outline-primary w-100">
            View details
          </a>
        </div>
      </div>
    </div>
  </div>
  <% }) %>
  <% } else { %>
  <div class="col">
    <div class="alert alert-info">No results found. Try adjusting your filters.</div>
  </div>
  <% } %>
</div>
